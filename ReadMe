Бот для karate kido 2

used libraries and modules:
    PIL, numpy, opencv-python, pywin32, tkinter

setups:
    -main.py: delay
    -bot.py: speed
Как запустить:

    #пока работает только при разрешении 1920 * 1080 при других разрешениях нужно вручную вводить координаты бревен

    установите все перечисленные пакеты (pip3 install {имя_пакета})

    запустите файл main.ru. Бот будет запущен в течении 5 секунд (при delay=5)

    за это время перейдите в браузер с игрой и наведите курсос на кнопку Play.

    ВНИМАНИЕ: игра должна быть запущена во весь экран (F11).

    Чтобы выйти нажмите "Q". (ENG, USA). Регистр не учитывается, раскладка да.

ToDo:
    автонаведение на конпку Play
    автоперезапуск после смерти
    корректировка координат бревен при разных разрешениях
    сбор бонусов

Pull requests приветствуются!

